name: My First Workflow

on: [push, fork]

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome message
              run: echo "My First Github Actions Job"

            - name: List Files
              run: ls
            
            - name: Read Files
              run: cat README.md